void main() {//this kind of program have error in cpp language and if you want to remove the error then you have to use capture concept.
  int x = 10;
  int y = 20;
  var funObj = (int a) {
  
      print(a);
      print(x);
      print(y);
  };
  funObj(20);
}